<%= simple_form_for @team do |f| %>
  <%= f.input :name %>
  <%= f.input :pillar_id, collection: Pillar.all.map {|r| [r.name, r.id, {value: r.id}] } %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>